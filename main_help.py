"""Справка командной строки приложения."""

HELP_TEXT = """Использование:
  python main.py [имя_агента] [--loop-pause=N]
      запустить жизнь агента
  python main.py <имя_агента> reply
      открыть переписку; Enter отправляет, Shift+Enter/Ctrl+Enter переносят строку;
      новые ответы агента подгружаются автоматически; Ctrl+C/Ctrl+D — выйти
  python main.py [имя_агента] --help
      показать эту справку

Денежные команды (имя агента обязательно):
  python main.py <имя_агента> pay <единицы> \"<причина>\"
      начислить агенту положительное целое число единиц
  python main.py <имя_агента> balance
      показать доступный баланс и оплаченные циклы
  python main.py <имя_агента> history
      показать историю трат агента

Денежные команды работают без LLM-конфигурации и Docker. Агент должен уже
существовать: денежные команды не создают новых агентов.

В reply-режиме доступны стрелки, Home/End и Delete для редактирования.
Если терминал не различает Ctrl+Enter, используйте Ctrl+J для переноса строки.

Опции:
  --loop-pause=N  фиксированная пауза между итерациями в секундах
  --help, -h      показать эту справку
"""


def print_help() -> None:
    """Напечатать справку в стандартный вывод."""
    print(HELP_TEXT)
